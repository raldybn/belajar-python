
# Contoh penggunaan hash(), tidak perlu import library
print(hash("Python"))   # Output: nilai hash unik (bisa berbeda tiap eksekusi)
print(hash(12345))
print(hash((1, 2, 3)))  # Tuple immutable bisa di-hash